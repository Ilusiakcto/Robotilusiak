# OpenArm V1 Left Arm Configuration
# CAN interface for left arm (follower)

can_interface: can1

# Motor IDs for left arm (7 DOF + gripper)
arm:
  motor_ids: [1, 2, 3, 4, 5, 6, 7]
  
gripper:
  motor_id: 8

# Control parameters
control:
  kp: 30.0
  kd: 1.0
  timeout_ms: 10
